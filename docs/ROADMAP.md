# Kahu Roadmap

Small, user-visible improvements queued behind current work.

- **Mute a rule by ID** — the Feed's "Muted rules" modal can list and unmute,
  but a mute can only be created from a visible alert card. Add a rule-ID +
  reason + duration form to the modal so noisy rules can be muted proactively
  (same guardrails: never critical rules, never high/critical alerts).
