# /etc/sysctl.d/99-kahu.conf
# Required for OpenSearch (Wazuh indexer + Shuffle)
vm.max_map_count=262144

# Required for Redis (openvas-scanner + MISP)
vm.overcommit_memory=1

# Network tuning for high-throughput log ingestion
net.core.somaxconn=65535
net.ipv4.tcp_max_syn_backlog=65535
