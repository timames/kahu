# Kahu Demo Stack — copy to .env and fill in real values
# Generate secrets: openssl rand -hex 24

# Wazuh
WAZUH_INDEXER_PASSWORD=admin
WAZUH_API_PASSWORD=Kahu@Wazuh2026!

# MISP
MISP_DB_PASSWORD=changeme
MISP_ROOT_PASSWORD=changeme
MISP_BASEURL=https://localhost:8443

# Shuffle
SHUFFLE_PASSWORD=changeme

# DFIR-IRIS
IRIS_DB_PASSWORD=changeme
IRIS_SECRET=changeme-generate-a-real-key

# NetBox
NETBOX_DB_PASSWORD=changeme
NETBOX_SECRET=changeme-generate-a-real-key
NETBOX_ADMIN_PASSWORD=changeme

# CISO Assistant
CISO_DB_PASSWORD=changeme
CISO_SECRET=changeme-generate-a-real-key

# Keycloak
KEYCLOAK_DB_PASSWORD=changeme
KEYCLOAK_ADMIN_PASSWORD=changeme

# Vault (dev mode only)
VAULT_DEV_TOKEN=kahu-dev-token

# Grafana
GRAFANA_PASSWORD=changeme
